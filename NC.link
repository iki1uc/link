export const LINK = {
    modules: {},

    bind(name, ref) {
        this.modules[name] = ref;
    },

    fullSync(packet) {
        const out = {
            time: Date.now(),
            packet,
            sync: true
        };
        console.log("[NC.link FullSync] →", out);
        return out;
    }
};
